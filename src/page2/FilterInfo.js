import React from 'react';
import '../App.css';

function FilterInfo(props) {
	const { own } = props;
	return (
		<div>
			<div> {/*{own.toString()}*/}</div>
		</div>
	);
}

export default FilterInfo;
